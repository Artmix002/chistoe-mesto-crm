class AppConstants {
  static const appName = 'Чистое место';
  static const locale = 'ru_RU';
  static const timeZone = 'Europe/Moscow';
  static const maxCachedCalls = 100;
  static const maxAvitoAudioCacheBytes = 512 * 1024 * 1024;
  static const maxAuditEntries = 500;
  static const networkTimeout = Duration(seconds: 20);
  static const searchDebounce = Duration(milliseconds: 250);
}
